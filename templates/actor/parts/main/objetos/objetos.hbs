<div class="tab-content">
    <!-- Encabezado de la Lista -->
    <div class="items-list-header flexrow">
        <h3 class="item-name">{{localize "HOGWARTS.InventoryTitle"}}</h3>
        <div class="item-controls">
            <a class="item-control item-create" title="{{localize 'HOGWARTS.CreateItem'}}" data-type="objeto_magico">
                <i class="fas fa-plus"></i> {{localize "HOGWARTS.Add"}}
            </a>
        </div>
    </div>

    <!-- Lista de Objetos -->
    <ol class="items-list">
        {{#each inventory as |item id|}}
        <li class="item flexrow" data-item-id="{{item._id}}">
            <div class="item-image"><img src="{{item.img}}" title="{{item.name}}" width="24" height="24"/></div>
            <h4 class="item-name">{{item.name}} <span class="item-quantity">{{item.system.quantity}}</span></h4>
            <div class="item-controls">
                <a class="item-control item-edit" title="{{localize 'HOGWARTS.Edit'}}"><i class="fas fa-edit"></i></a>
                <a class="item-control item-delete" title="{{localize 'HOGWARTS.Delete'}}"><i class="fas fa-trash"></i></a>
            </div>
        </li>
        {{/each}}
    </ol>
</div>